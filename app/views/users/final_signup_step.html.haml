.container
  .main-container-outer
    %h1.main-header-title Just one last step
    %p You're almost there, we just need some last bits of information before you can start upvoting, commenting, and submitting songs.
    = form_for @user, html: { class: 'form-container' } do |f|
      - if @user.errors.any?
        .error_messages
          %ul
            - for message in @user.errors.full_messages
              %li= message
      - if @user.identities.any?
        = f.label :username, 'Username'
        = f.text_field :username, placeholder: 'Username', value: @user.identities.first.username, id: 'username', class: 'submit-input'
        = f.label :username, 'Email'
        = f.text_field :email, :placeholder => 'Email', id: 'email', class: 'submit-input'
      - else
        %label{ for: :avatar } Upload photo
        = image_tag(@user.avatar_url) if @user.avatar?
        = f.hidden_field(:avatar_cache)
        = hidden_field_tag 'final_signup_step', true
        = f.file_field(:avatar)

      = f.submit 'Signup', class: 'primary-action-button'