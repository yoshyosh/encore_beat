.main-container-outer
  %h1.main-header-title-margin-bot.main-header-title APPROVE BEATS
  .songs-list-container
    %h4.song-list-date-group TUESDAY AUG 16TH
    -@submissions.each do |submission|
      .song
        .upvote-container
          / .arrow-up
          / %p.vote-count= submission.upvotes.count
        .song-details-container
          %h3.song-list-title
            = link_to formatted_line_for_submission(submission), submission.url, target: "_blank"
          .song-submitter
            = image_tag "/assets/headsht.png", class: "song-submitter-avatar"
            %p.song-submitter-name Kevin Ko
            = link_to "Approve", edit_submission_path(submission), {remote: true, approved: 1}
            = link_to "Reject", edit_submission_path(submission), {remote: true, approved: 2}